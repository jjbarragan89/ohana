import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.jjbarragan89.ohana',
  appName: 'Ohana',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
